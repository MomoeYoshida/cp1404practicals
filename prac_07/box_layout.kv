BoxLayout:
    orientation: 'vertical'
#    orientation: 'horizontal'
    Button:
        text: 'Clear'  # 3. Change the first button so it says "Clear"
        on_press: app.handle_clear()
    TextInput:  # 8. Change button "two" to a text input field
        id: input_name
        text: ''
    Button:
        text: 'Greet'  # 3. Change the third button so it says "Greet"
        on_press: app.handle_greet()  # 4. Add a button handler (a callback for the press event)
        # This references a method in your main app class called handle_greet().
    Label:  # 1. Add a label below the third button
        id: output_label  # 6. Add an id to the label
        text: 'Enter your name'
        color: (1, 1, 0, 1)  # 2. Set the new label's text colour to yellow